---
title: 'Products'
description: 'Create and manage products, fees, and donations'
---

Products are the foundation of all payments in Orgo. Whether you're collecting membership fees, accepting donations, or selling merchandise, everything starts with creating a product.

## How to access

**Settings** → **Fees** → **Products**

Or: **Resources** → **Products**

---

## Product Types

Orgo supports different product types for different payment scenarios:

| Type | Description | Use Case |
|------|-------------|----------|
| **Fee** | Membership/subscription fees | Annual membership, monthly dues |
| **Donation** | Charitable contributions | Fundraising campaigns |
| **Commerce** | Physical/digital goods | Merchandise, books, gear |
| **Event** | Event tickets | Conference registration, workshops |
| **Manual Invoice** | Custom invoicing | Special charges, reimbursements |

---

## Creating a Product

<Steps>
  <Step title="Navigate to Products">
    Go to **Settings** → **Fees** → **Products**.
  </Step>
  <Step title="Click Create Product">
    Select the type of product to create.
  </Step>
  <Step title="Enter basic information">
    Fill in name, description, and upload a logo/image.
  </Step>
  <Step title="Configure payment options">
    Set up pricing, subscriptions, and billing cycles.
  </Step>
  <Step title="Save">
    Save to create the product.
  </Step>
</Steps>

---

## Product Settings

### Basic Information

<ParamField path="name" type="string" required>
  Product display name shown to members and on payment pages.
</ParamField>

<ParamField path="nameEn" type="string">
  English name for multilingual organizations.
</ParamField>

<ParamField path="description" type="richtext">
  Detailed description explaining what the product is and what members get.
</ParamField>

<ParamField path="descriptionInternal" type="text">
  Internal notes for administrators (not shown to members).
</ParamField>

<ParamField path="logo" type="image">
  Product image/logo displayed on payment pages.
</ParamField>

<ParamField path="type" type="select" required>
  Product type (fee, donation, commerce, event, manual_invoice).
</ParamField>

### Pricing

<ParamField path="productPrices" type="array">
  List of price tiers for this product. Create multiple tiers for different membership levels or amounts.
</ParamField>

<ParamField path="hasCustomPriceValue" type="boolean">
  Allow payers to enter a custom amount (useful for donations).
</ParamField>

<ParamField path="customMinPrice" type="number">
  Minimum amount when custom pricing is enabled.
</ParamField>

<ParamField path="customMaxPrice" type="number">
  Maximum amount when custom pricing is enabled.
</ParamField>

### Payment Options

<ParamField path="hasSubscription" type="boolean">
  Enable recurring subscription payments.
</ParamField>

<ParamField path="hasOneTimePayment" type="boolean">
  Enable one-time payments.
</ParamField>

<ParamField path="monthPeriod" type="number" default="1">
  Billing period in months (1 = monthly, 12 = yearly).
</ParamField>

### Billing Cycle

<ParamField path="cycleDay" type="number">
  Day of month when billing cycles start (1-28).
</ParamField>

<ParamField path="cycleMonth" type="number">
  Month when annual cycle starts (1-12).
</ParamField>

<ParamField path="cycleHasProratio" type="boolean">
  Enable prorated billing for mid-cycle subscriptions.
</ParamField>

<ParamField path="proratioMinMonthsEnabler" type="number">
  Minimum months remaining to enable proration.
</ParamField>

<ParamField path="cycleRecurrences" type="number">
  Number of billing cycles before subscription ends (null = indefinite).
</ParamField>

### Dates

<ParamField path="startingDate" type="date">
  When the product becomes available.
</ParamField>

<ParamField path="maxDate" type="date">
  When the product expires/closes.
</ParamField>

### Visibility

<ParamField path="isExternal" type="boolean" default="false">
  Make product available to non-members (public page).
</ParamField>

<ParamField path="isInternal" type="boolean" default="true">
  Make product available to organization members.
</ParamField>

<ParamField path="displayOnProfile" type="boolean">
  Show product on member profile pages.
</ParamField>

<ParamField path="isArchived" type="boolean">
  Archive product (hides from public but keeps data).
</ParamField>

---

## Price Tiers

Each product can have multiple price tiers:

### Creating Price Tiers

1. Open the product
2. Go to **Prices** tab
3. Click **Add Price**
4. Enter:
   - Tier name (e.g., "Student", "Regular", "Family")
   - Amount
   - Currency
5. Save

### Example Tier Structure

```
Membership Fee Product
├── Student Tier - $50/year
├── Regular Tier - $100/year
├── Family Tier - $150/year
└── Lifetime Tier - $1,000 one-time
```

---

## Product Options

Add customizable options to products:

1. Open the product
2. Go to **Options** tab
3. Create options like:
   - T-shirt size
   - Meal preference
   - Workshop selection

Options allow members to customize their purchase.

---

## Public Payment Pages

Each product can have a public page for accepting payments:

### URL Structure

- By slug: `https://app.orgo.space/public/product/{slug}`
- By UUID: `https://app.orgo.space/public/product/{uuid}`

### Page Customization

Configure how the public page looks:

<ParamField path="settings.tenantLogo" type="boolean" default="true">
  Show organization logo on payment page.
</ParamField>

<ParamField path="settings.showTitle" type="boolean" default="true">
  Display product title prominently.
</ParamField>

<ParamField path="settings.colorSelection" type="color">
  Accent color for the page.
</ParamField>

<ParamField path="settings.colorButton" type="color">
  Payment button color.
</ParamField>

<ParamField path="settings.lightThemeOnly" type="boolean">
  Force light theme regardless of user preference.
</ParamField>

<ParamField path="settings.commentSection" type="boolean">
  Enable comment/message section (useful for donations).
</ParamField>

<ParamField path="settings.newestAndTopDonationSection" type="boolean">
  Show recent and top donations (for donation campaigns).
</ParamField>

---

## Goal Tracking

For donation campaigns or fundraising:

<ParamField path="goalValue" type="number">
  Target amount for one-time donations.
</ParamField>

<ParamField path="goalValueSubscription" type="number">
  Target amount for subscription donations.
</ParamField>

Progress toward goals is displayed on public pages.

---

## Local Center Products

Products can be created at the Local Center level:

<ParamField path="localCenter" type="relation">
  Link product to a specific Local Center.
</ParamField>

<ParamField path="hasLocalCenterOption" type="boolean">
  Allow payers to select which Local Center receives funds.
</ParamField>

---

## Use Cases

<AccordionGroup>
  <Accordion title="Annual membership fee">
    Create a fee product with yearly billing cycle and multiple tiers for different member types.
  </Accordion>

  <Accordion title="Monthly subscription">
    Set up a fee with monthly period and automatic recurring billing.
  </Accordion>

  <Accordion title="Fundraising campaign">
    Create a donation product with custom amounts, goal tracking, and public display of donors.
  </Accordion>

  <Accordion title="Event registration">
    Use event-type product with multiple price tiers for different ticket types.
  </Accordion>

  <Accordion title="Merchandise store">
    Set up commerce products with options for sizes/colors.
  </Accordion>
</AccordionGroup>

---

## Best Practices

<AccordionGroup>
  <Accordion title="Clear naming">
    Use descriptive names that clearly indicate what the product is and what period it covers.
  </Accordion>

  <Accordion title="Comprehensive descriptions">
    Include all relevant details about what's included and any terms.
  </Accordion>

  <Accordion title="Appropriate tiers">
    Create price tiers that match your organization's membership structure.
  </Accordion>

  <Accordion title="Archive, don't delete">
    Archive old products instead of deleting to preserve payment history.
  </Accordion>
</AccordionGroup>

---

## Related Features

- [Fees](/platform/fees/fees) - Fee management
- [Stripe Integration](/platform/fees/stripe-integration) - Payment processing
- [Donations](/platform/fees/donations) - Donation campaigns
- [Local Center Fees](/platform/local-group-fees/create-local-fees) - Local payments
